# 📱 Как запустить мобильное приложение EverFreeNote

## ✅ Что сделано

1. **Проект настроен** в `ui/mobile/`
2. **TypeScript и ESLint** настроены и проходят проверку
3. **Metro Bundler запущен** и готов к работе

---

## 🚀 Способы запуска

### **1. Expo Go (САМЫЙ ПРОСТОЙ)** ⭐ Рекомендуется для начинающих

#### Шаг 1: Установите Expo Go на телефон
- **Android**: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)
- **iOS**: [App Store](https://apps.apple.com/app/expo-go/id982107779)

#### Шаг 2: Запустите проект
```powershell
cd ui/mobile
npm start
```

#### Шаг 3: Подключитесь
1. В терминале появится **QR-код**
2. Откройте **Expo Go** на телефоне
3. Нажмите **"Scan QR code"**
4. Наведите камеру на QR-код
5. Приложение загрузится!

**💡 Важно:** Телефон и компьютер должны быть в **одной WiFi сети**

**Если не работает:** Используйте tunnel режим
```powershell
npm run start:tunnel
```

---

### **2. Android Emulator** (нужен Android Studio)

#### Шаг 1: Установите Android Studio
1. Скачайте [Android Studio](https://developer.android.com/studio)
2. Установите с настройками по умолчанию

#### Шаг 2: Создайте виртуальное устройство
1. Откройте Android Studio
2. **Tools** → **Device Manager**
3. **Create Device** → выберите Pixel 5
4. Скачайте System Image (например, Android 13)
5. **Finish**

#### Шаг 3: Запустите эмулятор
1. Нажмите ▶️ рядом с устройством в Device Manager
2. Дождитесь загрузки эмулятора

#### Шаг 4: Запустите приложение
```powershell
cd ui/mobile
npm run android
```

Expo автоматически найдет эмулятор и установит приложение!

---

### **3. Физическое устройство через ADB** (продвинутый)

#### Шаг 1: Включите режим разработчика на Android
1. **Настройки** → **О телефоне**
2. Нажмите 7 раз на **"Номер сборки"**
3. Появится сообщение "Вы стали разработчиком"

#### Шаг 2: Включите отладку по USB
1. **Настройки** → **Для разработчиков**
2. Включите **"Отладка по USB"**

#### Шаг 3: Подключите телефон
1. Подключите телефон по USB
2. Разрешите отладку на телефоне (всплывающее окно)

#### Шаг 4: Проверьте подключение
```powershell
adb devices
```

Должно показать ваше устройство.

#### Шаг 5: Запустите приложение
```powershell
cd ui/mobile
npm run android
```

**Проблема с ADB не найден?** Добавьте в PATH:
```
C:\Users\ВашИмя\AppData\Local\Android\Sdk\platform-tools
```

---

## 🛠️ Команды для разработки

### Запуск
```powershell
npm start              # Обычный режим
npm run start:tunnel   # Tunnel режим (если не работает WiFi)
npm run android        # Запуск на Android
npm run ios            # Запуск на iOS (только Mac)
```

### Проверка кода (АВТОМАТИЧЕСКАЯ)
```powershell
npm run validate       # TypeScript + ESLint (обязательно перед коммитом!)
npm run type-check     # Только TypeScript
npm run lint           # Только ESLint
```

---

## 📖 Интерфейс Expo Dev Server

После `npm start` в терминале появится меню:

```
› Press a │ open Android
› Press i │ open iOS simulator
› Press w │ open web (не работает в нашем проекте)
› Press j │ open debugger
› Press r │ reload app
› Press m │ toggle menu
```

**Горячие клавиши:**
- `a` - запустить на Android
- `i` - запустить на iOS (только Mac)
- `r` - перезагрузить приложение
- `j` - открыть Chrome DevTools для дебага

---

## ❓ Частые проблемы

### 1. "QR код не сканируется"
**Решение:**
```powershell
npm run start:tunnel
```

### 2. "Metro Bundler не запускается"
**Решение:**
```powershell
# Очистите кеш
npx expo start --clear
```

### 3. "Красный экран с ошибками"
**Решение:**
- Проверьте ошибки в терминале
- Нажмите `r` в Expo Dev Server для перезагрузки
- Или встряхните телефон → "Reload"

### 4. "Cannot connect to Metro"
**Проверьте:**
- Телефон и компьютер в одной WiFi сети
- Firewall не блокирует порт 8081
- Попробуйте tunnel режим

---

## 🎯 Что дальше?

После успешного запуска вы увидите:
- ✅ Экран логина (кнопка "Войти через Google")
- ✅ После нажатия → экран со списком заметок (2 тестовые)
- ✅ Tabs внизу: "Заметки" и "Настройки"
- ✅ При клике на заметку → экран редактора (пока заглушка)

---

## 📝 Следующие шаги разработки

1. ✅ Проект запущен
2. ⏳ Создать адаптеры (Storage, OAuth, Navigation)
3. ⏳ Реализовать EditorWebView компонент
4. ⏳ Подключить к Supabase
5. ⏳ Реализовать offline режим

---

## 💡 Советы

- **Используйте Expo Go** для начала - это проще всего
- **Встряхните телефон** чтобы открыть Dev Menu
- **Fast Refresh** включен - изменения применяются автоматически
- **Всегда запускайте `npm run validate`** перед коммитом

---

**Нужна помощь?** Просто спросите! 😊
