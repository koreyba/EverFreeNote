# Mobile UI layer (React Native / Expo)

Placeholder for mobile entrypoint, providers, and adapters (AsyncStorage, Linking/expo-auth-session) and the mobile Supabase client factory. Core logic should be reused from `/core`.
