# Mobile UI layer (React Native / Expo)

Placeholder for mobile entrypoint, providers, and adapters (AsyncStorage, Linking/expo-web-browser; OAuth flow TODO to wire real Supabase provider URL and deep-link handling) and the mobile Supabase client factory. Core logic should be reused from `/core`.
