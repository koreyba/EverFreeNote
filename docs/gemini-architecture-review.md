# Отчет об аудите архитектуры EverFreeNote

## 📦 Ревизия 1
> **Дата:** 28 ноября 2025
> **Аудитор:** GitHub Copilot (Gemini 3 Pro)
>
> ## 📊 Общее резюме
> Проект написан на современном стеке (Next.js 15, React 19, Supabase, React Query). В целом код чистый, типизация TypeScript используется корректно. Использование `React Query` для управления состоянием сервера — отличное решение, которое сильно упростит жизнь в будущем.
>
> **Соответствие инфраструктуре:** Архитектура полностью соответствует ограничениям Cloudflare Pages (Free Tier), работая как чистое SPA. Прямое взаимодействие с Supabase исключает необходимость в собственном бэкенде или серверных функциях (Next.js API Routes), что идеально для бесплатного тарифа.
>
> Однако, есть **критическая архитектурная проблема**: вся логика приложения (State Management, UI Orchestration, Business Logic) свалена в один файл `app/page.tsx`. Это "God Component" (Божественный компонент), который будет очень сложно поддерживать и невозможно переиспользовать в React Native.
>
> ---
>
> ## 🔍 Детальный аудит
>
> ### 1. Архитектура и Структура (Web)
> *   **✅ Плюсы:**
>     *   Использование **BaaS (Supabase)** напрямую с клиента — отлично для стартапа и SPA.
>     *   **React Query** (`useNotesQuery`, `useNotesMutations`) грамотно инкапсулирует работу с API. Эти хуки можно будет перенести в React Native почти без изменений.
>     *   Использование **виртуализации** (`react-window`) для списков — правильный подход к производительности.
>
> *   **❌ Минусы:**
>     *   **God Component (`app/page.tsx`):** Файл содержит более 500 строк. Он управляет всем: авторизацией, поиском, редактированием, удалением, диалогами и рендерингом всех частей UI.
>     *   **Смешивание ответственности:** В одном файле перемешана логика работы с данными (вызов мутаций), логика UI (какое окно показать) и верстка.
>
> ### 2. Проблемные места и Баги
> *   **Rich Text Editor (`tiptap`):** Используется библиотека `tiptap`, которая работает **только в браузере** (DOM-based).
>     *   *Риск:* В React Native этот редактор работать не будет. Вам придется либо использовать WebView (что медленно), либо искать нативный аналог (например, `Expensify/react-native-live-markdown` или `10play/10tap-editor`), что потребует конвертации форматов данных.
> *   **Браузерные API:** В коде встречаются прямые вызовы `window.prompt` (в `RichTextEditor.tsx`), `window.location.origin`, `localStorage`.
>     *   *Риск:* Это сломает React Native приложение, так как там нет объекта `window`.
> *   **Безопасность:** Использование `dangerouslySetInnerHTML` для рендеринга контента заметок. Хотя используется `DOMPurify` (в `page.tsx`), это всегда потенциальное место для XSS, если где-то забыть санитайзер.
>
> ### 3. Готовность к React Native (Android)
> Чат GPT был прав — текущая архитектура UI **не готова** к быстрому переносу.
>
> | Компонент | Готовность | Комментарий |
> |-----------|------------|-------------|
> | **Data Layer** | ⭐⭐⭐⭐⭐ | Хуки `useNotesQuery`, `useNotesMutations` отличные. Их можно переиспользовать на 90%. |
> | **Business Logic** | ⭐⭐ | Логика "размазана" по `page.tsx`. Её нужно вынести в отдельные хуки-контроллеры. |
> | **UI Components** | ⭐ | `shadcn/ui` и `Tailwind` (в чистом виде) не работают в React Native. Придется переписывать верстку на `NativeWind` или `StyleSheet`. |
> | **Editor** | ❌ | `tiptap` не совместим. Это будет самая сложная часть миграции. |
>
> ---
>
> ## 🚀 Рекомендации (Action Plan)
>
> ### Шаг 1: Декомпозиция `page.tsx` (Приоритет: Высокий)
> Разбей огромный файл на логические компоненты. Это нужно сделать **сейчас**, пока проект не разросся.
> *   `components/Sidebar.tsx` (список, поиск, профиль)
> *   `components/NoteEditor.tsx` (форма редактирования)
> *   `components/NoteView.tsx` (просмотр заметки)
> *   `components/EmptyState.tsx`
>
> *Важно:* При декомпозиции сохраняйте директиву `'use client'`, чтобы не нарушить работу статического экспорта (SPA). (форма редактирования)
> *   `components/NoteView.tsx` (просмотр заметки)
> *   `components/EmptyState.tsx`
>
> ### Шаг 2: Выделение логики (Controller Pattern)
> Создай хук `useNoteAppController`, который заберет всю логику из `page.tsx`.
> ```typescript
> // hooks/useNoteAppController.ts
> export function useNoteAppController() {
>   // Вся логика стейта (selectedNote, isEditing, searchQuery)
>   // Все обработчики (handleSave, handleDelete, handleSearch)
>   
>   return {
>     state: { user, selectedNote, isEditing, ... },
>     actions: { selectNote, saveNote, deleteNote, ... }
>   };
> }
> ```
> *Зачем:* В React Native ты просто импортируешь этот хук и подключишь его к нативным компонентам View/Text. Логика останется общей!
>
> ### Шаг 3: Абстракция платформенных зависимостей
> Замени прямые вызовы браузерных API на обертки.
> *   Вместо `window.prompt` → используй кастомный модальный диалог (он будет работать везде).
> *   Вместо `localStorage` → используй адаптер, который на вебе юзает localStorage, а в RN — `AsyncStorage` или `mmkv`.
>
> ### Шаг 4: Решение по Rich Text
> Заранее реши, что делать с редактором в мобилке.
> *   **Вариант А (Простой):** Использовать Markdown вместо HTML. Markdown легче рендерить нативно.
> *   **Вариант Б (Сложный):** Оставить HTML/Tiptap, но в React Native использовать компонент WebView для редактора.
>
> ### Итог
> Проект живой и рабочий, но архитектура UI монолитна. Если начнешь писать React Native приложение сейчас — придется дублировать всю логику. Если сделаешь рефакторинг (Шаг 1 и 2), сэкономишь до 40% времени на разработке мобильного приложения.
