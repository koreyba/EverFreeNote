---
phase: requirements
title: Requirements & Problem Definition
description: What problem are we solving and why?
---

# Requirements & Problem Definition

## Problem Statement
**What problem does this solve?**

Текущее состояние e2e тестов:
- ✅ Есть 3 e2e файла (auth-and-notes, basic-notes, fts-search)
- ❌ Тесты дублируют функциональность, покрытую компонентными тестами
- ❌ Нет системного подхода к e2e тестированию
- ❌ Отсутствует покрытие критических user flows
- ❌ Тесты не оптимизированы (много дублирования)

**Проблемы:**
1. Дублирование: компонентные тесты уже проверяют UI элементы, но e2e тесты повторяют те же проверки
2. Неполное покрытие: не покрыты критические флоу (импорт ENEX, теги, удаление заметок, infinite scroll)
3. Неоптимальная архитектура: каждый тест создает новые заметки вместо расширения существующих флоу
4. Отсутствие Page Object паттерна: код дублируется между тестами

## Goals
**What are we trying to achieve?**

### Primary Goals
1. **100% покрытие критических user flows** e2e тестами
2. **Нулевое дублирование** с компонентными тестами
3. **Оптимальная архитектура** e2e тестов (Page Objects, fixtures, helpers)
4. **Быстрое выполнение**: каждый тест ≤ 3 минуты

### Success Criteria
- [ ] Все критические user flows покрыты e2e тестами
- [ ] Нет дублирования проверок с компонентными тестами
- [ ] Используется Page Object паттерн
- [ ] Все тесты выполняются ≤ 3 минуты каждый
- [ ] Общее время выполнения всех e2e тестов ≤ 10 минут
- [ ] Документация архитектуры e2e тестов

## User Stories
**Who will use this and how?**

### Story 1: Разработчик добавляет новую фичу
**As a** разработчик  
**I want** чтобы e2e тесты проверяли только интеграционные флоу  
**So that** я могу быстро убедиться, что новая фича не сломала критические пути пользователя

**Acceptance Criteria:**
- E2e тесты покрывают end-to-end флоу (от входа до выхода)
- Компонентные тесты покрывают изолированную логику компонентов
- Нет дублирования между e2e и компонентными тестами

### Story 2: QA инженер проверяет релиз
**As a** QA инженер  
**I want** запустить e2e тесты перед релизом  
**So that** убедиться что все критические флоу работают

**Acceptance Criteria:**
- Все e2e тесты проходят за ≤ 10 минут
- Тесты покрывают все критические user journeys
- Понятные сообщения об ошибках

### Story 3: Новый разработчик пишет e2e тест
**As a** новый разработчик  
**I want** использовать готовые Page Objects и helpers  
**So that** быстро написать новый e2e тест без дублирования кода

**Acceptance Criteria:**
- Документация по архитектуре e2e тестов
- Примеры использования Page Objects
- Reusable helpers для типовых действий

## Scope

### In Scope
**What will we build?**

#### Критические User Flows (покрываем e2e):
1. **Authentication Flow**
   - Skip auth → работа с заметками → logout
   - Google OAuth flow (если возможно в e2e)

2. **Notes CRUD Flow**
   - Создание заметки → редактирование → удаление
   - Работа с тегами (добавление, удаление, фильтрация)
   - Infinite scroll (загрузка большого количества заметок)

3. **Search Flow**
   - Поиск заметок (FTS + fallback)
   - Фильтрация по тегам
   - Комбинированный поиск (текст + теги)

4. **Import Flow**
   - Импорт ENEX файла
   - Обработка дубликатов (prefix/skip)
   - Отображение прогресса импорта

5. **Theme Flow**
   - Переключение темы (light/dark)
   - Сохранение выбора темы

#### Архитектура:
1. **Page Objects** для всех страниц
2. **Fixtures** для тестовых данных
3. **Custom Commands** для типовых действий
4. **Test Helpers** для setup/teardown

### Out of Scope
**What won't we build?**

- ❌ Дублирование компонентных тестов (UI элементы, валидация форм, стилизация)
- ❌ Unit-тестирование отдельных функций
- ❌ Тестирование edge cases (покрыто компонентными тестами)
- ❌ Performance тестирование (отдельная задача)
- ❌ Accessibility тестирование (отдельная задача)

## Constraints & Assumptions

### Technical Constraints
- Cypress 15.5.0
- Next.js 15 (App Router)
- Supabase (auth + database)
- Время выполнения одного теста ≤ 3 минуты

### Assumptions
- Supabase доступен для тестов (локальный или тестовый инстанс)
- Skip auth доступен для быстрого входа
- ENEX файлы доступны для импорта
- Тесты запускаются в headless режиме в CI/CD

### Dependencies
- Компонентные тесты уже покрывают UI элементы
- Supabase RLS настроен корректно
- FTS индексы созданы в БД

## Open Questions
**What do we still need to figure out?**

1. ❓ Как тестировать Google OAuth в e2e? (mock или реальный flow?)
2. ❓ Нужно ли создавать отдельную тестовую БД или использовать локальный Supabase?
3. ❓ Как обрабатывать cleanup после тестов? (удалять созданные заметки?)
4. ❓ Нужны ли visual regression тесты?
5. ❓ Как тестировать offline mode (если есть)?

## Non-Functional Requirements

### Performance
- Каждый e2e тест ≤ 3 минуты
- Общее время всех e2e тестов ≤ 10 минут
- Параллельное выполнение тестов (если возможно)

### Reliability
- Тесты должны быть стабильными (no flaky tests)
- Retry механизм для нестабильных операций
- Понятные error messages

### Maintainability
- Page Object паттерн для переиспользования
- DRY принцип (no code duplication)
- Документация архитектуры

## Existing E2E Tests Analysis

### auth-and-notes.cy.js
**Покрывает:**
- ✅ Authentication UI (Skip auth)
- ✅ Создание заметки с rich text
- ✅ Редактирование заметки
- ✅ Множественные заметки + поиск
- ✅ Logout

**Проблемы:**
- ❌ Дублирует компонентные тесты (UI элементы, форматирование)
- ❌ Не покрывает удаление заметок
- ❌ Не покрывает работу с тегами
- ❌ Не покрывает импорт

### basic-notes.cy.js
**Покрывает:**
- ✅ Edge cases (пустая заметка, длинный заголовок, спецсимволы)

**Проблемы:**
- ❌ Полностью дублирует компонентные тесты
- ❌ Должно быть в компонентных тестах, не в e2e

### fts-search.cy.js
**Покрывает:**
- ✅ FTS API тестирование
- ✅ Highlighting
- ✅ Multi-language search
- ✅ Ranking

**Проблемы:**
- ❌ Тестирует API напрямую (должно быть в API тестах)
- ❌ Не тестирует UI поиска (SearchResults компонент)

## Recommended E2E Test Structure

### Новая структура (оптимизированная):

```
cypress/e2e/
├── user-journeys/
│   ├── complete-workflow.cy.js    # Полный флоу: auth → CRUD → search → logout
│   ├── import-workflow.cy.js      # Импорт ENEX → работа с импортированными заметками
│   └── theme-workflow.cy.js       # Переключение темы → проверка сохранения
├── critical-paths/
│   ├── notes-crud.cy.js           # Создание → редактирование → удаление (расширенный)
│   ├── tags-management.cy.js      # Работа с тегами (добавление, удаление, фильтрация)
│   └── infinite-scroll.cy.js      # Загрузка большого количества заметок
└── integration/
    ├── search-integration.cy.js   # UI поиска + фильтрация (не API)
    └── auth-integration.cy.js     # OAuth flow (если возможно)
```

### Принципы новой структуры:
1. **user-journeys/** - полные end-to-end флоу (от входа до выхода)
2. **critical-paths/** - критические функции (CRUD, теги, scroll)
3. **integration/** - интеграция между компонентами

### Что удаляем/рефакторим:
- ❌ `basic-notes.cy.js` → удалить (дублирует компонентные тесты)
- 🔄 `auth-and-notes.cy.js` → рефакторить в `complete-workflow.cy.js`
- 🔄 `fts-search.cy.js` → рефакторить в `search-integration.cy.js` (UI, не API)

