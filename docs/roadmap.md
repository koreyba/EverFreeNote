# 🌿 EverFreeNote — Технический путеводитель проекта

## ⭐ Концепция и видение

**EverFreeNote** — современное приложение для заметок, построенное по принципам простоты, автономности и расширяемости. Главная идея — объединить удобство локальных заметок и надёжность облачного хранения. Аналог Evernote в более упрощенном варианте.

---

## 🧭 Главная цель
Создать лёгкое и интуитивное приложение, где пользователь может:
- писать и редактировать заметки с любого устройства (в действительности: веб сайт + приложение андроид, но это позже, нужно просто заложить такую архитектурную возможность);
- иметь мгновенную синхронизацию через облако;
- входить по своему аккаунту (например, Google);
- хранить данные надёжно, безопасно и независимо.

---

## 🧱 Архитектура на высоком уровне

**1) Интерфейс (Frontend)**

* **React (Vite)** — динамичный UI и быстрая сборка.
* **Tailwind CSS** — адаптивный дизайн и единый визуальный язык.
* **shadcn/ui + Lucide React** — готовые компоненты и иконки.
* **Хостинг: Cloudflare Pages** — быстрый CDN и автодеплой.

**2) Бэкенд (BaaS)**

* **Supabase** — управляемая база данных, аутентификация и файловое хранилище.
* **OAuth-провайдер (Google)** — вход пользователей без собственной регистрации.
* Доступ к данным — **напрямую из фронтенда** через Supabase SDK (без собственного сервера).

**3) Обмен данными и безопасность**

* Фронтенд обращается к API Supabase (CRUD, Auth, Storage).
* Безопасность обеспечивается политиками доступа на стороне Supabase (Row Level Security).

---

## 💡 Принципы и идеология

1. **Простота на поверхности, мощь внутри.** UI — минималистичный, платформа — надёжная и масштабируемая.
2. **Безопасность по умолчанию.** Доступ к данным ограничен правилами на уровне BaaS.
3. **Один источник истины.** Все данные живут в облаке (Supabase), локальное хранилище — только кэш.
4. **Постепенное развитие без переписывания.** Архитектура сразу позволяет безопасно расширяться.
5. **Совместимость и открытость.** Стек на открытых технологиях; переносимость и локальный запуск — из коробки.

---

## 🧩 Минимальный состав системы (MVP)

Что должно работать в первой версии:

* Вход по аккаунту (Google) или гостевой режим.
* CRUD заметок (создание, редактирование, удаление).
* Автосохранение и синхронизация между устройствами.
* Хранение данных в облаке (Supabase).
* Простой интерфейс (список заметок + редактор).

---

## 🚀 Дорожная карта развития

| Этап    | Цель            | Ключевые улучшения                |
| ------- | --------------- | --------------------------------- |
| **MVP** | Базовый продукт | CRUD, вход, синхронизация         |
| **V2**  | Расширение      | Поиск по тексту, теги/категории   |
| **V3**  | Медиа           | Вложения (изображения/файлы)      |
| **V4**  | Офлайн          | Кэширование и авто‑синхронизация  |
| **V5**  | Мобайл          | Приложение на React Native / Expo |
| **V6**  | Интеллект       | Поиск по смыслу, AI‑подсказки     |

---

## 🧠 Основные технические принципы

* **KISS:** не перегружать архитектуру и интерфейс.
* **Clean Data Flow:** односторонний поток данных от облака к UI, чёткие границы модулей.
* **Security by Design:** минимальные права по умолчанию; доступ только к своим данным.
* **Performance First:** быстрая загрузка, кэширование, оптимизированные бандлы, lazy‑loading.
* **Extensibility:** модульная структура, лёгкое добавление новых фич.

---

## 🧭 Организация разработки

1. **Frontend** — модульные компоненты, единые UI‑паттерны, дизайн‑система на Tailwind.
2. **BaaS (Supabase)** — управление через панель и/или миграции (при необходимости).
3. **OAuth и интеграции** — настраиваются централизованно в Supabase.
4. **CI/CD** — Cloudflare Pages автодеплоит при изменениях в репозитории.
5. **Документация** — данный файл хранится в `/docs/TECH_PLAN.md` и обновляется по мере развития.

---

## 🧩 Локальное развёртывание и тестирование

Приложение разворачивается **локально** для разработки и тестирования в окружении, максимально близком к продакшену.

### 🎯 Цель

* Быстро запускать проект локально без ручной настройки сервисов.
* Тестировать всё (Auth, CRUD, Storage) без постоянных деплоев.
* Сохранять воспроизводимость окружения между разработчиками.

### ⚙️ Практики и инструменты

**1) Supabase CLI + Docker Desktop**

* Используем локальный стек Supabase (БД, Auth, Storage) в контейнерах.
* Базовые команды:

  * `supabase init` — подготовка проекта к локальному запуску.
  * `supabase start` — поднять локальные сервисы в Docker.
  * `supabase stop` — остановить локальные сервисы.
  * `supabase db reset` — чистый старт БД (для интеграционных тестов).

**2) Frontend (Vite + React)**

* Локальный сервер разработки (`pnpm dev`).
* Конфигурация берётся из локальных переменных окружения (файл `.env.local`, не коммитится).

**3) Переменные окружения**

* Для локальной разработки применяются отдельные значения (URL/ключи) — хранятся только локально и исключены из Git.
* Для продакшена переменные задаются в панели Cloudflare Pages (не хранить в репозитории).

**4) Миграции и бэкапы**

* Структурные изменения БД оформляются миграциями (по мере надобности).
* Локальные бэкапы и восстановление — через Supabase CLI.
* Принцип: изменения схемы — всегда через скрипт/миграцию, а не ручные правки.

**5) Контейнеризация фронта (опционально)**

* Для полной переносимости можно добавить `Dockerfile` и `docker-compose.yml`, чтобы поднимать фронт и локальный Supabase одной командой.
* Это гарантирует одинаковое окружение у всех участников.

**6) Тестирование**

* Е2Е‑сценарии (критические пользовательские потоки) выполняются локально (перед релизом).
* Смоук‑чек: вход, создание/редактирование/удаление заметки, обновление на втором устройстве.

### ✅ Результат

* Разработчик разворачивает проект локально «в один клик» (CLI + Docker).
* Проверяются все фичи без деплоя в облако.
* Окружение воспроизводимо и близко к продакшену.

---

## ☁️ Cloudflare R2 — будущее расширение

Cloudflare R2 рассматривается как дополнительный слой для масштабирования хранения файлов.

### 🔹 Назначение

Используется для дешёвого и надёжного хранения больших файлов (изображения, вложения, резервные копии). Совместимо с протоколом S3 и легко интегрируется с Supabase или напрямую из фронтенда.

### 🔹 Когда подключать

* После выхода за пределы бесплатного лимита Supabase Storage.
* При росте проекта, когда требуется хранение больших медиа-данных.
* Для организации резервных копий и CDN-доставки через Cloudflare.

### 🔹 Архитектура интеграции

1. Supabase остаётся ядром для БД, Auth и логики доступа.
2. R2 выступает как отдельный слой хранения.
3. Загрузка файлов идёт напрямую в R2, а ссылки сохраняются в Supabase.
4. Безопасность обеспечивается через подписанные URL или сервисные токены.

### 🔹 Преимущества

* Низкая стоимость хранения и трафика.
* Простая масштабируемость.
* Интеграция с Cloudflare CDN для быстрой доставки контента.
* Полная совместимость с S3-совместимыми SDK.

---

## 🌐 Итоговая идея

EverFreeNote — это не клон, а лёгкий и современный «персональный блокнот», где облако, интерфейс и пользователь работают как единое целое:
📱 быстро, 🔒 безопасно, ☁️ синхронно, 💡 гибко.
