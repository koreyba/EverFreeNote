# 🏗️ EverFreeNote — Архитектурная документация

> **Дата создания:** 17 октября 2025  
> **Версия:** 1.0  
> **Тип приложения:** Single Page Application (SPA)

---

## 📐 Общая архитектура

### Тип приложения: **SPA (Single Page Application)**

**Принцип:** Одна HTML-страница загружается один раз, все взаимодействия происходят через JavaScript без полной перезагрузки страницы.

### Технологический стек

```
┌─────────────────────────────────────────┐
│         Frontend (Client-Side)          │
├─────────────────────────────────────────┤
│  • Next.js 14 (App Router)              │
│  • React 18                              │
│  • Tailwind CSS + shadcn/ui              │
│  • Static Export (output: 'export')     │
└─────────────────────────────────────────┘
                    ↓ HTTPS
┌─────────────────────────────────────────┐
│         Backend (BaaS)                   │
├─────────────────────────────────────────┤
│  • Supabase (PostgreSQL)                 │
│  • Supabase Auth (OAuth)                 │
│  • Supabase Storage                      │
│  • Row Level Security (RLS)              │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         Hosting                          │
├─────────────────────────────────────────┤
│  • Cloudflare Pages                      │
│  • Static Files (HTML/CSS/JS)            │
│  • Edge CDN                              │
└─────────────────────────────────────────┘
```

---

## 🎯 Ключевые архитектурные решения

### 1. **Статический экспорт (Static Export)**

**Конфигурация:**
```javascript
// next.config.js
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  trailingSlash: true,
}
```

**Что это значит:**
- ✅ Next.js генерирует чистые HTML/CSS/JS файлы
- ✅ Нет серверного рендеринга (SSR)
- ✅ Нет API Routes
- ✅ Деплой на любой статический хостинг
- ✅ Все страницы помечены как `'use client'`

### 2. **Прямое подключение к Supabase**

**Архитектурный паттерн: BaaS (Backend as a Service)**

```javascript
// Все операции с данными происходят напрямую из браузера
const supabase = createClient()

// Примеры операций
await supabase.from('notes').select('*')           // Чтение
await supabase.from('notes').insert(data)          // Создание
await supabase.from('notes').update(data)          // Обновление
await supabase.from('notes').delete()              // Удаление
```

**Безопасность:**
- Row Level Security (RLS) на уровне Supabase
- Пользователи видят только свои данные
- Политики доступа настроены на бэкенде

### 3. **Клиентская маршрутизация**

**Правила:**
- ❌ **НЕ используем** динамические роуты (`[param]`) с `output: 'export'`
- ✅ **Используем** клиентское состояние для фильтрации/навигации
- ✅ **Используем** query parameters для шаринга состояния (опционально)

**Пример:**
```javascript
// ❌ НЕПРАВИЛЬНО: /app/tags/[tag]/page.js (конфликт с static export)

// ✅ ПРАВИЛЬНО: Фильтрация в основном компоненте
const [filterByTag, setFilterByTag] = useState(null)
const handleTagClick = (tag) => {
  setFilterByTag(tag)
  fetchNotes('', tag)
}
```

---

## 📊 Работа с данными

### Принципы загрузки данных

#### ❌ **НЕ ДЕЛАТЬ:**
```javascript
// Загрузка ВСЕХ записей
const { data } = await supabase.from('notes').select('*')
```

#### ✅ **ПРАВИЛЬНО:**

**1. Пагинация**
```javascript
const PAGE_SIZE = 50

const fetchNotes = async (page = 0) => {
  const { data } = await supabase
    .from('notes')
    .select('*')
    .order('updated_at', { ascending: false })
    .range(page * PAGE_SIZE, (page + 1) * PAGE_SIZE - 1)
  
  return data
}
```

**2. Серверная фильтрация**
```javascript
// Фильтрация на Supabase, а не в JavaScript
let query = supabase.from('notes').select('*')

if (tagFilter) {
  query = query.contains('tags', [tagFilter])
}

if (searchQuery) {
  query = query.or(`title.ilike.%${searchQuery}%,description.ilike.%${searchQuery}%`)
}

const { data } = await query
```

**3. Ленивая загрузка (Infinite Scroll)**
```javascript
const [notes, setNotes] = useState([])
const [page, setPage] = useState(0)
const [hasMore, setHasMore] = useState(true)

const loadMoreNotes = async () => {
  const newNotes = await fetchNotes(page)
  if (newNotes.length < PAGE_SIZE) setHasMore(false)
  setNotes(prev => [...prev, ...newNotes])
  setPage(prev => prev + 1)
}
```

### Оптимизация производительности

**Для больших списков (100+ элементов):**
- Используйте виртуализацию (`react-window` или `react-virtualized`)
- Рендерить только видимые элементы

**Для поиска:**
- Дебаунсинг (задержка 300-500мс перед запросом)
- Отмена предыдущих запросов (AbortController)

---

## 🔐 Безопасность

### Row Level Security (RLS)

**Все политики настраиваются в Supabase:**

```sql
-- Пример: пользователи видят только свои заметки
CREATE POLICY "Users can view own notes"
  ON notes FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own notes"
  ON notes FOR INSERT
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own notes"
  ON notes FOR UPDATE
  USING (auth.uid() = user_id);

CREATE POLICY "Users can delete own notes"
  ON notes FOR DELETE
  USING (auth.uid() = user_id);
```

### Аутентификация

- OAuth через Supabase (Google, GitHub, и т.д.)
- Session management управляется Supabase SDK
- Tokens хранятся в localStorage (автоматически)

---

## 📦 Структура проекта

```
EverFreeNote/
├── app/
│   ├── page.js              # Главная страница (единая точка входа)
│   ├── layout.js            # Общий layout
│   ├── globals.css          # Глобальные стили
│   └── auth/
│       └── callback/
│           └── page.js      # OAuth callback
├── components/
│   ├── InteractiveTag.jsx   # Переиспользуемые компоненты
│   └── ui/                  # shadcn/ui компоненты
├── lib/
│   ├── supabase/
│   │   └── client.js        # Supabase client singleton
│   └── utils.js             # Утилиты
├── docs/
│   ├── ARCHITECTURE.md      # Этот файл
│   ├── DEPLOYMENT_GUIDE.md  # Инструкции по деплою
│   └── roadmap.md           # Roadmap развития
├── next.config.js           # Next.js конфигурация
└── package.json
```

---

## 🚀 Деплой на Cloudflare Pages

### Build Configuration

```bash
# Build Command
npm run build

# Build Output Directory
out/

# Node Version
18 или выше
```

### Environment Variables

Установите в Cloudflare Pages Settings:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

### Процесс деплоя

1. **Push в репозиторий** → Cloudflare автоматически запускает build
2. **Build процесс:**
   ```
   npm install
   npm run build
   → Генерируется папка out/ со статическими файлами
   ```
3. **Deploy** → Статические файлы публикуются на Cloudflare Edge CDN
4. **Готово** → Приложение доступно по URL

---

## 🎨 Правила разработки новых фич

### Контрольный чек-лист для новых фич

#### ✅ Перед добавлением любой фичи проверьте:

1. **Не требуется ли серверный код?**
   - Если да → используйте Supabase Functions или Database Functions
   - Если нет → реализуйте на клиенте

2. **Нужны ли динамические роуты?**
   - Если да → используйте клиентское состояние вместо роутинга
   - Пример: фильтрация, модалки, табы

3. **Будет ли загружаться много данных?**
   - Если да → реализуйте пагинацию + серверную фильтрацию
   - Используйте `.range()` и `.limit()`

4. **Нужна ли реал-тайм синхронизация?**
   - Используйте Supabase Realtime:
     ```javascript
     supabase
       .channel('notes-changes')
       .on('postgres_changes', { event: '*', schema: 'public', table: 'notes' }, 
         (payload) => handleRealtimeUpdate(payload)
       )
       .subscribe()
     ```

5. **Совместимо ли с `output: 'export'`?**
   - Проверьте, что не используете:
     - API Routes (`/app/api/`)
     - Server Actions
     - Server Components с серверной логикой
     - `getServerSideProps`, `getStaticPaths` с параметрами

### Примеры правильной реализации

#### ✅ Фильтрация по категориям
```javascript
// НЕ создавайте /app/category/[slug]/page.js
// Используйте состояние:
const [activeCategory, setActiveCategory] = useState(null)
```

#### ✅ Модальные окна
```javascript
// НЕ создавайте отдельные роуты
// Используйте компоненты:
const [isModalOpen, setIsModalOpen] = useState(false)
<Dialog open={isModalOpen} onOpenChange={setIsModalOpen}>
```

#### ✅ Поиск
```javascript
// Серверная фильтрация + дебаунсинг
const debouncedSearch = useMemo(
  () => debounce((query) => fetchNotes(query), 300),
  []
)
```

---

## 🧪 Тестирование архитектуры

### Проверка статического экспорта

```bash
npm run build
# Должно создать папку out/ без ошибок
# Все роуты должны быть помечены как ○ (Static)
```

### Проверка совместимости с Cloudflare

```bash
# Локальный превью статических файлов
npx serve out/

# Проверьте:
# - Все страницы открываются
# - Нет 404 ошибок
# - API запросы к Supabase работают
```

---

## 📈 Масштабирование

### Когда проект вырастет

**Оптимизация производительности:**

1. **Code Splitting**
   ```javascript
   // Ленивая загрузка тяжелых компонентов
   const HeavyEditor = lazy(() => import('./HeavyEditor'))
   ```

2. **React Query / SWR**
   ```javascript
   // Кеширование и синхронизация данных
   const { data, isLoading } = useQuery(['notes', page], 
     () => fetchNotes(page)
   )
   ```

3. **Виртуализация**
   ```javascript
   import { FixedSizeList } from 'react-window'
   // Рендер только видимых элементов
   ```

4. **Optimistic Updates**
   ```javascript
   // Обновляем UI сразу, не ждем ответа сервера
   setNotes(prev => [...prev, newNote])
   await supabase.from('notes').insert(newNote)
   ```

---

## 🔄 Миграция в будущем (если понадобится)

### Если SPA перестанет подходить

**Сценарии когда может понадобиться SSR:**
- SEO критично важен для публичного контента
- Нужны server-side вычисления
- Требуется Edge Computing

**Путь миграции:**
1. Удалить `output: 'export'` из `next.config.js`
2. Добавить Cloudflare Next.js адаптер
3. Добавить API Routes для серверной логики
4. Перенести части логики в Server Components

**НО:** для приложения заметок (личных данных) SPA — оптимальное решение.

---

## 📝 Changelog архитектуры

| Дата       | Версия | Изменение                                    |
|------------|--------|----------------------------------------------|
| 2025-10-17 | 1.0    | Первая версия архитектурной документации     |

---

## 🎯 Заключение

**EverFreeNote = SPA + Supabase BaaS + Cloudflare Pages**

- ✅ Простая архитектура
- ✅ Легкий деплой
- ✅ Масштабируемость
- ✅ Низкая стоимость
- ✅ Высокая производительность

**Все новые фичи должны следовать этим принципам.**

При сомнениях — обратитесь к этому документу. 🚀

