/**
 * Test component for verifying MCP server development workflow.
 * Used as a target component when testing the MCP server integration.
 */
export default function TestTargetComponent() {
  return (
    <div data-testid="test-component">
      Test component loaded successfully
    </div>
  );
}
